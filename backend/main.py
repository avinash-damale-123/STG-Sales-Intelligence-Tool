from fastapi import FastAPI\nfrom fastapi.middleware.cors import CORSMiddleware\n\napp = FastAPI()\n\n# CORS configuration\napp.add_middleware(\n    CORSMiddleware,\n    allow_origins=["*"],  # Specify the origins you want to allow\n    allow_credentials=True,\n    allow_methods=["*"],  # Specify the methods you want to allow\n    allow_headers=["*"]  # Specify the headers you want to allow\n)\n\n@app.get("/health")\ndef health_check():\n    return {"status": "healthy"}